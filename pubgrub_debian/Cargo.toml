[package]
name = "pubgrub_debian"
version = "0.1.0"
edition = "2021"

[dependencies]
pubgrub = { git = "https://github.com/pubgrub-rs/pubgrub", branch = "dev", features = ["serde"] }
rustc-hash = "=1.1.0"
